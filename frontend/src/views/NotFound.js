import React from 'react';
import { Main } from '../styles/NotFoundStyles';

const NotFound = () => {
    return (
        <Main>
            <h5>Sorry! The URL that you are looking for does not exist.</h5>
        </Main>
    );
};

export default NotFound;