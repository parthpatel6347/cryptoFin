import React from 'react';
import { Main } from '../styles/NotFoundStyles';


const CoinError = () => {
    return (
        <Main>
            <h5>Sorry! Could not find coin with the given symbol.</h5>
        </Main>
    );
};

export default CoinError;